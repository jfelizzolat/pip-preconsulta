export type Variantes = "activo" | "inactivo" | "completo";
export type IPropsIndicadores = {
  titulo: string;
  variante: "activo" | "inactivo";
  completo: boolean;
};

// export interface IPropsIndicadores {
//   props: Indicador[];
// }
